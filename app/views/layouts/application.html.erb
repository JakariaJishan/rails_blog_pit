<!DOCTYPE html>
<html>
  <head>
    <title>RailsBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-[#001524] text-center text-white my-5">

  <!--  navigation-->
  <div class="flex justify-between items-center container mx-auto">
    <div>
      <%= link_to 'Home', root_path  %>
    </div>


    <div data-controller="modal"  class="relative">
      <button data-action="modal#open" class="">Open modal</button>

      <dialog data-modal-target="modal" id="modal" class="p-0 backdrop:bg-gray-400 backdrop:bg-opacity-50 open:animate-fade-in open:backdrop:animate-fade-in ">
        <button type="button" data-action="modal#close" class="">X</button>


        <hr class="mb-4">
        <% if user_signed_in? %>
          <div class="text-green-600">Signed in as... <%= current_user.username %></div>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:"bg-red-400 px-3 py-2" %>
        <% else %>
          <%= link_to 'Sign In', user_session_path, class:"bg-gray-400  px-3 py-2" %>
        <% end %>

      </dialog>
    </div>
  </div>



  <p class="notice text-gray-600"><%= notice %></p>
  <p class="alert text-red-400"><%= alert %></p>


  <div class="w-2/5 mx-auto">
    <%= yield %>
    </div>
  </body>
</html>
